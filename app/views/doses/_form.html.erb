<h1>Add a dose for <strong><%= @cocktail.name %></strong></h1>

<div class="dose_form">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient, allow_blank: :false %>
    <%= f.button :submit %>
  <% end %>
</div>
